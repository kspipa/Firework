#include "../../lib/api.h"
int main()
{
    printf("first_module has been started\n");
    module_struct *nne;
    int sock;
    nne->name = "first_module";
    nne->sock = sock;
    init_module(nne);
}
